const menuButtons=document.querySelectorAll("#contact menu button"),articles=document.querySelectorAll("#contact article");menuButtons.forEach((t,e)=>{t.addEventListener("click",()=>{menuButtons.forEach(t=>t.removeAttribute("aria-selected")),articles.forEach(t=>t.setAttribute("hidden",!0)),t.setAttribute("aria-selected",!0),articles[e].removeAttribute("hidden")})});